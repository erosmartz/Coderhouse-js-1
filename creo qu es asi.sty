tienen q ser dos script, uno para la lista deseados persistente q esta en todas las pag. y otro para la calculadora + el agregador a lista deseados


script 1 = calculadora. tiene el set_data para poner los datos en un array de objetos "juego" en el session storage, 
y tiene tambien otra funcion que es el pop_up. para mostrar el cartelito con los valores + iva y el boton de agregar a lista deseados, el boton este pushea el objeto que representa ese nodo, a un array JSON para localSTorage persistente

script 2 = lista deseados persistente. esta en todas las paginaas y tiene un creador de nodo para mostrar el array de JSON


para evitar q el boton de script1 se repita, tengo q hacer un if para que chequeee si el nodo no es null, que no se ejecute la creacion del nodo y en vez de eso se modifique el innerHTML

cuando se apreta el boton de agregar a deseados, se translada el objeto del array de session storage a un push al array persistente de JSON

